# Lab_Assignment_4
Lab Assignment 4 for OOP at BCC

Some assignment directions could not be followed directly

copy constructor doesn't work in an abstract class because it doesn't have objects

super.computeVolatility() cannot be called and is not needed


When ran the program produces an array of three contracts followed by their implied volatility arbitrarily "computed" by random number generation.
